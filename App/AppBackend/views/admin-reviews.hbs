
<div class="row mt-5">
    <div class="col-12">
        <h3>Reviews: {{reviews.length}}</h3>
        {{!-- <h3>Check-out Date:{{check_out_date}}</h3>
        <h3>Number Of Guests: {{guests}}</h3>
        <h2>Available Rooms: {{#if rooms}} {{rooms.length}} {{/if}}</h2> --}}
    </div>
</div>

{{#if reviews}}
    <div class="table-responsive-sm">
        <table class="table table-hover">
            <thead>
                <tr>
                    <th>User ID</th>
                    <th>Rating</th>
                    <th>Comments</th>
                  
                    
                    {{!-- <th class="d-print-none">
                        <a class="btn btn-sm btn-success" href="/add">Add</a>
                    </th> --}}
                </tr>
            </thead>
            <tbody>
                {{#each reviews}} 
                {{!-- mono gia ta available rooms!! --}}
                    <tr>
                        <td>{{this.UserUserId}}</td>
                        <td>{{this.Rating}}</td>
                        <td>{{this.Comments}}</td>
                        {{!-- <td>
                            <a class="btn btn-sm btn-secondary" href="/deleteBooking">Delete Booking</a>
                        </td> --}}
                    </tr>
                {{/each}}
            </tbody>
        </table>
    {{!-- <h3> {{users}}</h3> --}}
    </div>
{{/if}}
{{#unless reviews}}
    <div class="row mt-5">
        <div class="col">No Reviews Found!</div>
    </div>
    {{!-- <div class="row mt-2">
        <div class="col">
            <a class="btn btn-sm btn-success" href="/add">Προσθήκη</a>
        </div>
    </div> --}}
{{/unless}}
{{#if error}} 
    <div class="row mt-5">
        <div class="col-6 offset-3">
            <div class="alert alert-primary" role="alert">
                {{error}}
            </div>
        </div>
    </div>
{{/if}}